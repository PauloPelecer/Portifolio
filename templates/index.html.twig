<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
        <meta property="og:description" content="vai vim descrição ainda" />
        <!-- <meta property="og:image"  content="./images/imgLink.jpeg"/> -->
        <meta property="og:title"       content="vai vim title ainda" />
        <link rel="stylesheet" href="{{ url_for('static', filename='style/css/default.css') }}" />
        <!-- Adicione esses links no head -->
        <title>Devs-Absolutos</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    </head>
    <body>
      <center>
        <h5>&bull;Inicio &bull;Discord &bull;Softwares &bull;Contato</h5>
        <h1>Bem Vindos(as)</h1>
        <p>
        Essa Plataforma Foi Desenvolvida Para Estutandes de Programação<br/>
        Linguagens Usadas Para Criação Dessa Plataforma São:<br/>
        </p>
        
        <button type="button" id="btn-lang" class="btn-list btn-info" aria-haspopup="true" aria-expanded="false">
            Linguagens
        </button>
        <div id="list-lang" style="display: none;">
            {% for i in langs %}
                <div class="div-item-lang">{{ i }}</div>
                <div class="div-divider"></div>
            {% endfor %}
        </div>

        <br/>

        <button type="button" id="btn-pttn-me" class="btn-list btn-info" aria-haspopup="true" aria-expanded="false">
            Front-End By
        </button>
         <div id="list-me" style="display: none;">
            <img id="img-me"></img>
            {% for i in th %}
                <div onClick="window.location.href='{{ th[i] }}'" class="div-item-me"><a>{{ i }}</a></div>
                <div class="div-divider"></div>
            {% endfor %}
        </div>

        <br/>

        <button type="button" id="btn-pttn-look" class="btn-list btn-info" aria-haspopup="true" aria-expanded="false">
            Back-End By
        </button>
        <div id="list-look" style="display: none;">
        <img src='https://gitlab.com/uploads/-/system/project/avatar/14483828/development.browser.gears.-512.png' class='img-looc'></img>
            {% for i in look %}
                <div onClick="window.location.href='{{ look[i] }}'" class="div-item-me"><a>{{ i }}</a></div>
                <div class="div-divider"></div>
            {% endfor %}
        </div>
 
       
        <p>
            Nossa Plataforma Disponibiliza Softwares Para Estudos De Programação<br/>
            No Botão Abaixo Deixamos Os Scripts Criados pelos Desenvolvedor Da Plataforma Atual.<br/>
        </p>
        <input type="submit" value="Scripts"/>

        <h3>Cursos Recomendados Pelos Criadores:</h3>
        
        {% for curso in cursos %}
            <br/>{{curso}}<br/>
            <a href="{{cursos[curso]}}">
                <button id="btn-c" class="btn btn-success">Curso {{curso}}!</button>
            </a><br/>
        {% endfor %}
      </center>
    </body>
    <footer></footer>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <script src="https://unpkg.com/@popperjs/core@2/dist/umd/popper.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.3.js"></script>
    <script src="{{ url_for("static", filename="JS/list.js") }}" type="text/javascript"></script>
</html>
